// Good Luck 💪🏾
function reverse(doc) {
    const words = doc.split(" ");
    const gmcc1 = words.reverse();
    const rdoc = gmcc1.join(" ");
    return rdoc;
  }
  const doc = "";
 
  const reversed = reverse(doc);
  console.log(reverse("fikrcamp bootcamp"));
  console.log(reverse("hello there"));
  console.log(reverse("welcome to my city "));